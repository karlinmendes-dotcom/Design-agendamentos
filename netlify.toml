# Configuração de deploy da Barbearia Neto na Netlify
[build]
  command = "npm run build"
  publish = "dist"

# Redirect SPA: todas as rotas (/servicos, /agendamento, /admin...) servem o index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
